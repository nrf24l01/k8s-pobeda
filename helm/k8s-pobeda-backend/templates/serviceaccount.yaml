{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "k8s-pobeda-backend.serviceAccountName" . }}
  labels:
    {{- include "k8s-pobeda-backend.labels" . | nindent 4 }}
{{- end }}
